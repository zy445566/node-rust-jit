sudo: enabled

language: node_js


node_js:
  - 6
  - 8
  - stable

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - python2.7

script: curl -o rustup.sh https://sh.rustup.rs && sh rustup.sh -y && npm install -g neon-cli && node test 

after_success: npm i codecov && codecov